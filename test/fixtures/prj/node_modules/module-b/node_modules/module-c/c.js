'use strict';

var config = require('configg')(__dirname);

var M = function(){
	return { c: config };
}

module.exports = M;

if (module === require.main) {
	console.log(JSON.stringify(M(), null, 2));
}
